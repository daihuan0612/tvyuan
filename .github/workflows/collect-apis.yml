name: Collect APIs

on:
  schedule:
    - cron: "0 16 1 * *"  # 每月北京时间1日凌晨0点运行 (UTC 16点)
  workflow_dispatch:

jobs:
  collect-apis:
    runs-on: ubuntu-latest

    permissions:
      contents: write  # 允许推送

    steps:
      # 1️⃣ 检出仓库
      - uses: actions/checkout@v4

      # 2️⃣ 安装系统依赖 & Node.js
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y jq curl
          curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
          sudo apt-get install -y nodejs
          npm install bs58 axios

      # 3️⃣ 运行 API 搜集脚本
      - name: Run API collection
        run: |
          echo "开始搜集网络API资源..."
          node collect_apis.js

      # 4️⃣ 清理无法搜索的API
      - name: Clean unsearchable APIs
        run: |
          echo "清理无法搜索的API..."
          node clean_unsearchable_apis.js

      # 5️⃣ 如果配置文件有变化，则重新生成相关文件
      - name: Regenerate files if config changed
        run: |
          # 检查是否有变更
          if git diff --quiet LunaTV-config.json; then
            echo "配置文件无变化，跳过重新生成"
            exit 0
          fi
          
          echo "配置文件有变化，重新生成相关文件..."
          
          # 生成 jingjian.json
          jq '{
            cache_time: .cache_time,
            api_site: (
              .api_site
              | with_entries(select(.value._comment | not))
            )
          }' LunaTV-config.json > jingjian.json
          
          # 验证 jingjian.json
          jq empty jingjian.json
          
          # 生成 jin18.json（去掉 adult）
          jq '{
            cache_time: .cache_time,
            api_site: (
              .api_site
              | with_entries(select(.value.name | startswith("🔞") | not))
            )
          }' jingjian.json > jin18.json
          
          # 验证 jin18.json
          jq empty jin18.json
          
          # Base58 编码
          cat > encode.js <<'EOF'
          const fs = require('fs');
          const bs58 = require('bs58');

          const files = [
            { input: 'LunaTV-config.json', output: 'LunaTV-config.txt' },
            { input: 'jingjian.json', output: 'jingjian.txt' },
            { input: 'jin18.json', output: 'jin18.txt' }
          ];

          files.forEach(file => {
            if (!fs.existsSync(file.input)) return;
            const data = fs.readFileSync(file.input);
            const encoded = bs58.encode(Buffer.from(data));
            fs.writeFileSync(file.output, encoded);
          });
          EOF
          
          node encode.js
          
          # 运行 API 检查脚本
          node check_api.js "斗罗大陆"
          
          # 更新 README 或报告
          node update_readme.js

      # 5️⃣ 提交并推送所有生成文件
      - name: Commit and push all generated files
        run: |
          # 配置 Git
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          
          # 检查是否有变更
          if git diff --quiet; then
            echo "没有文件变化，无需提交"
            exit 0
          fi
          
          # 拉取远程 main 分支，避免冲突
          git fetch origin main
          git reset --soft origin/main

          # 添加所有生成文件
          git add LunaTV-config.json jingjian.json jin18.json LunaTV-config.txt jingjian.txt jin18.txt report.md README.md collection_report.md

          # 提交更新
          git commit -m "自动搜集并添加新API ($(date -d '+8 hour' +'%Y-%m-%d %H:%M:%S CST'))"

          # 推送到远程 main
          git push https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/${{ github.repository }}.git HEAD:main
